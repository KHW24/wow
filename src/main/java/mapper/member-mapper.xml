<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Member">
	<!-- resultMap의 id는 알아볼 수 있게 지정해야함 type은 vo의 루트를 입력하면됨 -->
	<resultMap id="resultMember" type="Member">
	
		<!-- vo에서 지정했던 필드들 모두 입력. property는 vo에서 썼던 필드이름이고, column은 SQL문에서의 테이블 컬럼명 입력해야함 -->
		<!-- 먼저 primary key 선언 -->
		<id property="id" column="ID" />	
		<result property="nickname" column="NICKNAME" />
		<result property="password" column="PASSWORD" />
		<result property="email" column="EMAIL" />
		<result property="address" column="ADDRESS" />
		<result property="alert_cnt" column="ALERT_CNT" />
		<result property="isAdmin" column="IS_ADMIN"/>
	</resultMap>
	
	<!-- INSERT -->
	<insert id="insertMember" parameterType="Member">
		INSERT INTO TABLE1 VALUES(#{id}, #{nickname}, #{password}, #{email}, #{address}, DEFAULT, DEFAULT)
	</insert>
	
	<!--UPDATE  -->
		
		
	<!-- 아이디 찾기, 닉네임과 이메일 입력해서 아이디 찾기 받아오는 값이 2개이기 떄문에 ParameterType값을 String으로 하지않고 Member(vo)로 작성 -->
	<select id="findId" parameterType="Member" resultMap="resultMember">
		SELECT ID FROM MEMBER WHERE NICKNAME = #{nickname} AND EMAIL = #{email}
	</select>
	
</mapper>